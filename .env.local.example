# The free demo, local quickstart. Same keys as .env.example, and both have working defaults in code,
# so leaving them blank is fine. Copy this to `.env.local`, which git ignores.
NEXT_PUBLIC_SITE_NAME=VILIHA
NEXT_PUBLIC_SITE_URL=https://viliha.com
